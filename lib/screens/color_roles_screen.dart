import 'package:flutter/material.dart';

class ColorRolesScreen extends StatelessWidget{
  const ColorRolesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}